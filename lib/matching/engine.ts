export type MatchUser = { id:string; full_name:string; city:string; neighborhood:string; is_verified:boolean; is_premium:boolean; reports_count:number; needed:number[]; duplicates:number[] };
export function computeMatches(current: MatchUser, candidates: MatchUser[]) {return candidates.map(c=>{const theyHaveForMe=c.duplicates.filter(n=>current.needed.includes(n));const iHaveForThem=current.duplicates.filter(n=>c.needed.includes(n));const sameCity=current.city===c.city;const sameNeighborhood=sameCity&&current.neighborhood===c.neighborhood;const score=theyHaveForMe.length*10+iHaveForThem.length*10+(sameCity?20:0)+(sameNeighborhood?15:0)+(c.is_verified?8:0)+(c.is_premium?5:0)-c.reports_count*3;return {user:c,theyHaveForMe,iHaveForThem,sameCity,sameNeighborhood,score,suggestedExchangeItems:[...theyHaveForMe.slice(0,5),...iHaveForThem.slice(0,5)]};}).filter(m=>m.theyHaveForMe.length>0).sort((a,b)=>b.score-a.score)}
